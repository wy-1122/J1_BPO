server:
  port: 8088
spring:
  thymeleaf:
    cache: false
    check-template: true
    check-template-location: true
    enabled: true
    encoding: utf-8
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html
  resources:
    static-locations: classpath:/static/
  mvc:
    static-path-pattern: /static/**
    view:
      prefix: /pages/
      suffix: .html

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bpo2?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: bpo
    password: StrongP@ss!
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  configuration:
    map-underscore-to-camel-case: true #数据库表中的下划线自动转换为驼峰
  #    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/**/*.xml
